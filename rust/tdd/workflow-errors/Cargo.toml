[package]
name = "workflow-errors-tdd"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
rust-version = "1.87"
description = "Expected-RED Rust tests translated from @workflow/errors before implementation"

[lib]
path = "src/lib.rs"

[workspace]

[lints.rust]
unsafe_code = "forbid"

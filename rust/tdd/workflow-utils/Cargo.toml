[package]
name = "workflow-utils-tdd"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
rust-version = "1.87"
description = "Expected-RED Rust tests translated from @workflow/utils before implementation"

[lib]
path = "src/lib.rs"

[workspace]

[dependencies]
workflow-utils = { path = "../../../crates/workflow-utils" }

[lints.rust]
unsafe_code = "forbid"

[package]
name = "workflow-core"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"
rust-version = "1.87"
description = "Rust implementation of the Workflow deterministic runtime"

[lib]
path = "src/lib.rs"

[workspace]

[dependencies]
semver = "=1.0.26"
workflow-world = { path = "../workflow-world" }

[lints.rust]
unsafe_code = "forbid"
